# Git and IDE
.git
.idea
*.iml
.vscode

# Build outputs (rebuilt inside container)
.gradle
build
*/build

# Docker
docker-compose*.yml
Dockerfile
.dockerignore

# Docs and local env
README.md
.env
.env.*
!.env.example

# Other services not needed when building individual service images (each Dockerfile copies only its module + events-schema)

# Scripts and config not needed for build
start-infra.sh
docker
